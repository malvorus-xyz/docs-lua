# engine

> csgo build only — unavailable in `sandbox.exe`.

## Reference

| Call | Description |
| --- | --- |
| `engine.exec(...)` | engine.exec(command) |
| `engine.get_local_player(...)` | engine.get_local_player(): entity \| nil |
| `engine.get_map_name(...)` | engine.get_map_name(): string |
| `engine.get_player_info(...)` | engine.get_player_info(index): table \| nil |
| `engine.get_view_angles(...)` | engine.get_view_angles(): vec3 |
| `engine.is_in_game(...)` | engine.is_in_game(): bool |
| `engine.set_view_angles(...)` | engine.set_view_angles(vec3) |
