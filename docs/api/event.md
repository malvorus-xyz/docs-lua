# event

> csgo build only — unavailable in `sandbox.exe`.

## Reference

| Call | Description |
| --- | --- |
| `event[...]` | — |
| `event:get_bool(...)` | — |
| `event:get_float(...)` | — |
| `event:get_int(...)` | — |
| `event:get_name(...)` | — |
| `event:get_string(...)` | — |
