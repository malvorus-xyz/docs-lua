# timer

## Reference

| Call | Description | Source |
| --- | --- | --- |
| `timer:is_active(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() | [TimerIsActive](base/scripts/api/utils.cpp#L368) |
| `timer:run_once(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() | [TimerRunOnce](base/scripts/api/utils.cpp#L339) |
| `timer:set_delay(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() | [TimerSetDelay](base/scripts/api/utils.cpp#L354) |
| `timer:start(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() | [TimerStart](base/scripts/api/utils.cpp#L310) |
| `timer:stop(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() | [TimerStop](base/scripts/api/utils.cpp#L325) |
