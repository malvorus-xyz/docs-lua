# timer

## Reference

| Call | Description |
| --- | --- |
| `timer:is_active(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() |
| `timer:run_once(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() |
| `timer:set_delay(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() |
| `timer:start(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() |
| `timer:stop(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() |
