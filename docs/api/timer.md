# timer

## Reference

| Call | Description | Source |
| --- | --- | --- |
| `timer:is_active(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() | [TimerIsActive](https://github.com/malvorus-xyz/base/blob/master/base/scripts/api/utils.cpp#L368) |
| `timer:run_once(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() | [TimerRunOnce](https://github.com/malvorus-xyz/base/blob/master/base/scripts/api/utils.cpp#L339) |
| `timer:set_delay(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() | [TimerSetDelay](https://github.com/malvorus-xyz/base/blob/master/base/scripts/api/utils.cpp#L354) |
| `timer:start(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() | [TimerStart](https://github.com/malvorus-xyz/base/blob/master/base/scripts/api/utils.cpp#L310) |
| `timer:stop(...)` | timer:start() / stop() / run_once() / set_delay(ms) / is_active() | [TimerStop](https://github.com/malvorus-xyz/base/blob/master/base/scripts/api/utils.cpp#L325) |
