# entity

> csgo build only — unavailable in `sandbox.exe`.

## Reference

| Call | Description |
| --- | --- |
| `entity[...]` | — |
| `entity:get_class(...)` | entity:get_class(): string |
| `entity:get_eye_position(...)` | entity:get_origin() / get_eye_position(): vec3 |
| `entity:get_hitbox_position(...)` | entity:get_hitbox_position(hitbox): vec3 |
| `entity:get_index(...)` | entity:get_index() / is_valid() / is_dormant() / is_player() / is_alive() / is_enemy() |
| `entity:get_origin(...)` | entity:get_origin() / get_eye_position(): vec3 |
| `entity:get_player_info(...)` | entity:get_player_info(): table \| nil |
| `entity:get_pointer(...)` | entity:get_pointer(): number |
| `entity:get_prop(...)` | entity:get_prop(name, element = 0): value |
| `entity:is_alive(...)` | entity:get_index() / is_valid() / is_dormant() / is_player() / is_alive() / is_enemy() |
| `entity:is_dormant(...)` | entity:get_index() / is_valid() / is_dormant() / is_player() / is_alive() / is_enemy() |
| `entity:is_enemy(...)` | entity:get_index() / is_valid() / is_dormant() / is_player() / is_alive() / is_enemy() |
| `entity:is_player(...)` | entity:get_index() / is_valid() / is_dormant() / is_player() / is_alive() / is_enemy() |
| `entity:is_valid(...)` | entity:get_index() / is_valid() / is_dormant() / is_player() / is_alive() / is_enemy() |
| `entity:set_prop(...)` | entity:set_prop(name, value, element = 0) |
