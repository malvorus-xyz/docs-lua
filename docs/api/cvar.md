# cvar

> csgo build only — unavailable in `sandbox.exe`.

## Reference

| Call | Description |
| --- | --- |
| `cvar[...]` | cvar['name']: csgo.cvar |
| `cvar:get_float(...)` | — |
| `cvar:get_int(...)` | — |
| `cvar:get_string(...)` | — |
| `cvar:set_float(...)` | — |
| `cvar:set_int(...)` | — |
| `cvar:set_string(...)` | — |
