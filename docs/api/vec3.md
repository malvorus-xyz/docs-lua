# vec3

## Reference

| Call | Description | Source |
| --- | --- | --- |
| `vec3[...]` | — | [Vec3Index](base/scripts/api/math.cpp#L58) |
| `vec3:cross(...)` | vec3:dot(other) / cross(other) / normalize() / unpack() | [Vec3Cross](base/scripts/api/math.cpp#L276) |
| `vec3:dot(...)` | vec3:dot(other) / cross(other) / normalize() / unpack() | [Vec3Dot](base/scripts/api/math.cpp#L264) |
| `vec3:length(...)` | vec3:length() / length2d() / length_sqr() / length2d_sqr() | [Vec3Length](base/scripts/api/math.cpp#L220) |
| `vec3:length2d(...)` | vec3:length() / length2d() / length_sqr() / length2d_sqr() | [Vec3Length2D](base/scripts/api/math.cpp#L231) |
| `vec3:length2d_sqr(...)` | vec3:length() / length2d() / length_sqr() / length2d_sqr() | [Vec3Length2DSqr](base/scripts/api/math.cpp#L253) |
| `vec3:length_sqr(...)` | vec3:length() / length2d() / length_sqr() / length2d_sqr() | [Vec3LengthSqr](base/scripts/api/math.cpp#L242) |
| `vec3:normalize(...)` | vec3:dot(other) / cross(other) / normalize() / unpack() | [Vec3Normalize](base/scripts/api/math.cpp#L287) |
| `vec3:unpack(...)` | vec3:dot(other) / cross(other) / normalize() / unpack() | [Vec3Unpack](base/scripts/api/math.cpp#L297) |
