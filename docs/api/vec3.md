# vec3

## Reference

| Call | Description |
| --- | --- |
| `vec3[...]` | — |
| `vec3:cross(...)` | vec3:dot(other) / cross(other) / normalize() / unpack() |
| `vec3:dot(...)` | vec3:dot(other) / cross(other) / normalize() / unpack() |
| `vec3:length(...)` | vec3:length() / length2d() / length_sqr() / length2d_sqr() |
| `vec3:length2d(...)` | vec3:length() / length2d() / length_sqr() / length2d_sqr() |
| `vec3:length2d_sqr(...)` | vec3:length() / length2d() / length_sqr() / length2d_sqr() |
| `vec3:length_sqr(...)` | vec3:length() / length2d() / length_sqr() / length2d_sqr() |
| `vec3:normalize(...)` | vec3:dot(other) / cross(other) / normalize() / unpack() |
| `vec3:unpack(...)` | vec3:dot(other) / cross(other) / normalize() / unpack() |
