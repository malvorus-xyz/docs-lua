# entities

> csgo build only — unavailable in `sandbox.exe`.

## Reference

| Call | Description |
| --- | --- |
| `entities[...]` | entities[index]: entity \| nil |
| `entities.for_each(...)` | entities.for_each(callback(entity)) |
| `entities.for_each_player(...)` | entities.for_each_player(callback(entity)) |
| `entities.get_entity(...)` | entities.get_entity(index): entity \| nil |
